import React, {FC} from 'react'

interface Props {
    open: boolean;
    setOpen: (open: boolean) => void;
    activeItem: number;
}

const header:FC<Props> = (props) => {
    return (
        <div>
            
        </div>
    )
}

export default header
